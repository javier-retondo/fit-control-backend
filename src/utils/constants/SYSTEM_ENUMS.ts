export enum STATUS {
   ACTIVE = 0,
   INACTIVE = 1,
}
export enum PROCESS_TYPE {
   SEND_SURVEY = 1,
   SEND_EMAIL = 2,
   SEND_PUBLISH = 3,
}
export enum METHOD_SEND {
   EMAIL = 1,
   PUBLISH = 2,
   PUSH = 3,
}
